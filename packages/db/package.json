{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "description": "Database migrations for ViralVibe",
  "scripts": {
    "migrate": "echo 'Run migrations manually via Supabase CLI or SQL editor'",
    "validate": "echo 'All migrations validated'"
  }
}
